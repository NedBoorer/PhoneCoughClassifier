name: phone_cough_classifier_app
description: Mobile client for the Phone Cough Classifier.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Networking
  http: ^1.1.0
  
  # Audio Recording
  record: ^5.0.0
  path_provider: ^2.1.1
  permission_handler: ^11.0.0
  
  # UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
